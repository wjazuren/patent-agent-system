7月13号更新

1.添加了真实数据，来源于谷歌中国的5万条数据
保存格式如下：
params = (
        patent_data["patent_number"],
        patent_data["title"],
        patent_data["applicant"],
        patent_data["application_date"],
        patent_data["abstract"],
        patent_data["technical_field"],
        txt_path
    )

保存在db文件中，其中txt_path为保存专利全文的路径

2.构建chroma向量库
使用BM25算法来进行检索，使用该算法原因是：BM25是基于频次的稀疏词检索算法，更适合该项目